<controls:MetroWindow x:Class="Barebones.Pages.MessageBox.DialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Barebones.Pages.MessageBox"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
            TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            Background="{DynamicResource MaterialDesignPaper}"
            BorderBrush="{DynamicResource MaterialDesignTextBoxBorder}" 
            GlowBrush="{DynamicResource AccentColorBrush}"
            BorderThickness="1"
            TextElement.FontWeight="Medium"
            TextElement.FontSize="18"
            FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
            mc:Ignorable="d" 
            Height="250" 
            Width="350"
            d:DataContext="{d:DesignInstance local:DialogViewModel}">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock HorizontalAlignment="Center">This is a custom dialog view</TextBlock>
        <TextBlock HorizontalAlignment="Center" Text="{Binding Message}"></TextBlock>
    </StackPanel>
</controls:MetroWindow>

